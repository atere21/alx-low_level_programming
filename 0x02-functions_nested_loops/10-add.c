#include "main.h"
/**
 * add - adds two integers
 * @a: first integer
 * @b: second integer
 *
 * Return: addition of int one and int two
 */
int add(int one, int two)
{
	return (one + two);
}
